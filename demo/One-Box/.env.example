# Copy to demo/One-Box/.env and adjust for your environment.
# ONEBOX_API_TOKEN is injected into requests made by the UI when running via Docker.
ONEBOX_API_TOKEN=demo-superuser-token

# Use an Anvil default private key for quickstart demos. Replace with a secure key in production.
ONEBOX_RELAYER_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# Optional: override orchestrator address exposed to the browser.
ORCHESTRATOR_URL=http://orchestrator:8080/onebox

# Allow the UI origin in CORS checks.
ONEBOX_CORS_ALLOW=http://localhost:4173

# Optional: surface transaction links in the UI.
EXPLORER_TX_BASE=https://etherscan.io/tx

# Optional: override the default IPFS gateway for specs/deliverables.
IPFS_GATEWAY_BASE=https://ipfs.io/ipfs
