name: apps-images

on:
  pull_request:
    paths:
      - 'apps/**'
      - '.github/workflows/apps-images.yml'
  push:
    branches: [main]
    paths:
      - 'apps/**'
      - '.github/workflows/apps-images.yml'

permissions:
  contents: read
  packages: write
  security-events: write
  id-token: write

concurrency:
  group: apps-${{ github.ref }}
  cancel-in-progress: true

jobs:
  console:
    runs-on: ubuntu-24.04
    outputs:
      namespace: ${{ steps.namespace.outputs.value }}
      digest: ${{ steps.get_digest.outputs.digest }}
    steps:
      - uses: actions/checkout@v4

      - name: Derive GHCR namespace
        id: namespace
        run: |
          VALUE="${GITHUB_REPOSITORY_OWNER,,}"
          echo "value=$VALUE" >> "$GITHUB_OUTPUT"
          echo "GHCR_NAMESPACE=$VALUE" >> "$GITHUB_ENV"

      - name: Login to GHCR (main)
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Console image
        id: build_console
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/console/Dockerfile
          platforms: linux/amd64
          push: ${{ github.ref == 'refs/heads/main' }}
          load: ${{ github.ref != 'refs/heads/main' }}
          tags: |
            ghcr.io/${{ steps.namespace.outputs.value }}/owner-console:${{ github.sha }}
            ghcr.io/${{ steps.namespace.outputs.value }}/owner-console:latest
          provenance: false
          sbom: false
          outputs: type=docker

      - name: Get Console digest (main)
        id: get_digest
        if: github.ref == 'refs/heads/main'
        run: |
          REF="ghcr.io/${GHCR_NAMESPACE}/owner-console:${{ github.sha }}"
          DIGEST=$(docker buildx imagetools inspect "$REF" | awk -F'[: ]+' '/Digest:/ {print $2; exit}')
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Trivy (OS – enforce)
        run: |
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            aquasec/trivy:0.53.0 image \
              --vuln-type os \
              --severity HIGH,CRITICAL \
              --exit-code 1 \
              ghcr.io/${GHCR_NAMESPACE}/owner-console:${{ github.sha }}

      - name: Trivy (libraries – audit only)
        continue-on-error: true
        run: |
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            aquasec/trivy:0.53.0 image \
              --vuln-type library \
              --severity HIGH,CRITICAL \
              --exit-code 0 \
              ghcr.io/${GHCR_NAMESPACE}/owner-console:${{ github.sha }}

  portal:
    runs-on: ubuntu-24.04
    outputs:
      namespace: ${{ steps.namespace.outputs.value }}
      digest: ${{ steps.get_digest.outputs.digest }}
    steps:
      - uses: actions/checkout@v4

      - name: Derive GHCR namespace
        id: namespace
        run: |
          VALUE="${GITHUB_REPOSITORY_OWNER,,}"
          echo "value=$VALUE" >> "$GITHUB_OUTPUT"
          echo "GHCR_NAMESPACE=$VALUE" >> "$GITHUB_ENV"

      - name: Setup Node for web build
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: 'npm'

      - name: Install root deps
        run: npm ci

      - name: Webapp build
        run: npm --prefix apps/enterprise-portal run build

      - name: Login to GHCR (main)
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build Portal image
        id: build_portal
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/enterprise-portal/Dockerfile
          platforms: linux/amd64
          push: ${{ github.ref == 'refs/heads/main' }}
          load: ${{ github.ref != 'refs/heads/main' }}
          tags: |
            ghcr.io/${{ steps.namespace.outputs.value }}/webapp:${{ github.sha }}
            ghcr.io/${{ steps.namespace.outputs.value }}/webapp:latest
          provenance: false
          sbom: false
          outputs: type=docker

      - name: Get Portal digest (main)
        id: get_digest
        if: github.ref == 'refs/heads/main'
        run: |
          REF="ghcr.io/${GHCR_NAMESPACE}/webapp:${{ github.sha }}"
          DIGEST=$(docker buildx imagetools inspect "$REF" | awk -F'[: ]+' '/Digest:/ {print $2; exit}')
          echo "digest=$DIGEST" >> "$GITHUB_OUTPUT"

      - name: Trivy (OS – enforce)
        run: |
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            aquasec/trivy:0.53.0 image \
              --vuln-type os \
              --severity HIGH,CRITICAL \
              --exit-code 1 \
              ghcr.io/${GHCR_NAMESPACE}/webapp:${{ github.sha }}

      - name: Trivy (libraries – audit only)
        continue-on-error: true
        run: |
          docker run --rm \
            -v /var/run/docker.sock:/var/run/docker.sock \
            aquasec/trivy:0.53.0 image \
              --vuln-type library \
              --severity HIGH,CRITICAL \
              --exit-code 0 \
              ghcr.io/${GHCR_NAMESPACE}/webapp:${{ github.sha }}

  provenance-console:
    if: github.ref == 'refs/heads/main'
    needs: [console]
    permissions:
      actions: read
      id-token: write
      contents: write
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.0.0
    with:
      base64-subjects: ${{ toJson(format('ghcr.io/{0}/owner-console@{1}', needs.console.outputs.namespace, needs.console.outputs.digest)) }}

  provenance-portal:
    if: github.ref == 'refs/heads/main'
    needs: [portal]
    permissions:
      actions: read
      id-token: write
      contents: write
    uses: slsa-framework/slsa-github-generator/.github/workflows/generator_generic_slsa3.yml@v2.0.0
    with:
      base64-subjects: ${{ toJson(format('ghcr.io/{0}/webapp@{1}', needs.portal.outputs.namespace, needs.portal.outputs.digest)) }}
